// "Needs update" / version-check badge removed (upstream branding).
interface VersionBadgeProps {
  currentVersion?: string
  className?: string
}

export function VersionBadge(_props: VersionBadgeProps) {
  return null
}
