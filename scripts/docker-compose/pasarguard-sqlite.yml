services:
  pasarguard:
    image: ghcr.io/alirezanorouzzadeh9/pasarguard:latest
    restart: always
    env_file: .env
    network_mode: host
    volumes:
      - /var/lib/pasarguard:/var/lib/pasarguard
